name: ccusage perf comment

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  compare:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 15

    steps:
      - name: Checkout PR
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Checkout base
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          ref: ${{ github.event.pull_request.base.sha }}
          path: base

      - name: Setup Nix
        uses: ./.github/actions/setup-nix

      - name: Build base ccusage
        run: nix develop --command pnpm --filter ccusage run build
        working-directory: base

      - name: Build PR ccusage
        run: nix develop --command pnpm --filter ccusage run build

      - name: Compare fixture performance
        run: |
          nix develop --command ./apps/ccusage/scripts/compare-pr-performance.ts \
            --base-dir base \
            --head-dir . \
            --fixture-dir apps/ccusage/test/fixtures/claude \
            --warmup 2 \
            --runs 7 \
            --output "$RUNNER_TEMP/ccusage-perf-comment.md"

      - name: Upsert PR comment
        env:
          COMMENT_FILE: ${{ runner.temp }}/ccusage-perf-comment.md
          COMMENT_MARKER: <!-- ccusage-perf-comment -->
          GITHUB_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: nix develop --command ./.github/scripts/upsert-pr-comment.ts
